version: '1.0.0'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_KEY=dev_api_key
      - RATE_LIMIT_MAX=100
    volumes:
      - ./:/app
    command: uvicorn simplebank.main:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped 